import { CopyOutlined, LinkOutlined } from "@ant-design/icons"
import { Button, Card, Descriptions, Space } from "antd"

export const DetailItemSekeleton = () => {
  return (
    <Card title={""} style={{ backgroundColor: "#f5f5f5" }} loading={true}>
    </Card>
  )
}